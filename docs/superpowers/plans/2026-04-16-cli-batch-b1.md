# 批次 B1 Implementation Plan — Typer 遷移 + 既有指令回歸

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 把整個 `threads` CLI 從 argparse 遷移到 Typer，保留所有既有行為（publish / reply / publish-chain），並先建立 subprocess 黑箱測試層作為 regression 防線。

**Architecture:** 保留 `threads_cli/cli.py` 當 entry point（pyproject.toml `threads` script 指向此），但內部改成 Typer app 宣告 + subcommand groups 載入。`publish` / `reply` / `publish-chain` 三個既有 handler 拆到獨立 `post.py` module。既有 87 tests 因為 handler signature 改變必須重寫；但 subprocess 黑箱測試層跨越 argparse/Typer 邊界不變，**是唯一真正的 regression 防線**。

**Tech Stack:** Python 3.13+、Typer（新增依賴）、pytest、argparse（遷移後移除）、現有 `publisher.py` / `safety.py` / `output.py`（不動）

**Branch:** `feat/cli-batch-b1`（新分支，不在 main 直接做）

**相關 spec**: `docs/superpowers/specs/2026-04-16-cli-batch-b-design.md`

---

## File Structure

### 不動
- `publisher.py`（repo root）
- `threads_cli/safety.py`（四層安全）
- `threads_cli/output.py`（`emit` / `error` / `warn`——B1 不擴充 warnings，B2 才加）
- `threads_cli/__init__.py`
- `threads_cli/SKILL.md`
- `tests/test_cli_safety.py`（不依賴 argparse Namespace，不用改）
- `tests/test_publisher.py`（不依賴 CLI，不用改）

### 修改
- `pyproject.toml`（Task 1 加 `typer` 依賴；`[project.scripts]` entry 不動）
- `threads_cli/cli.py`（Task 4 改寫為 Typer app 宣告 + 掛載 groups）
- `tests/test_threads_cli.py`（Task 8 重寫所有 in-process case 配合 Typer handler signature）

### 新增
- `threads_cli/post.py`（Task 4 建骨架、Task 5/6/7 填入 publish / reply / publish-chain）
- `threads_cli/posts.py`（Task 4 建空骨架——B2 填 search / list）
- `threads_cli/account.py`（Task 4 建空骨架——B2 填 info / insights）
- `tests/test_cli_blackbox.py`（Task 2 建立的 subprocess 黑箱測試層）

### 删除
- 無（所有現有檔案保留）

---

## Task 1: 加 typer 依賴並驗證安裝

**Files:**
- Modify: `pyproject.toml`

- [ ] **Step 1: 開新分支**

```bash
git checkout -b feat/cli-batch-b1
git status
```

Expected: `On branch feat/cli-batch-b1`，clean working tree。

- [ ] **Step 2: 修改 pyproject.toml 加入 typer 依賴**

把 `pyproject.toml` 的 `dependencies` 區塊從：

```toml
dependencies = [
    "requests>=2.31",
    "pyyaml>=6.0",
    "jinja2>=3.1",
]
```

改為：

```toml
dependencies = [
    "requests>=2.31",
    "pyyaml>=6.0",
    "jinja2>=3.1",
    "typer>=0.12",
]
```

（保留其他區塊原樣——`[project.scripts]`、`package-dir` 等都不動。）

- [ ] **Step 3: 重新安裝（editable）**

```bash
pip install -e ".[dev]"
```

Expected: 無 error。`typer` 被安裝（終端會看到 `Successfully installed typer-X.Y.Z ...`）。

**⚠ 注意（批次 A lessons 1.2）**：若此步把 pytest 意外升版而破壞 pytest-asyncio，跑：

```bash
pip install -U pytest-asyncio
```

- [ ] **Step 4: 驗證 import 成功**

```bash
python -c "import typer; print(typer.__version__)"
```

Expected: 印出版本號（例如 `0.12.3` 或更新）。

- [ ] **Step 5: 驗證既有 tests 全綠（baseline）**

```bash
python -m pytest
```

Expected: 87 tests 全 pass（加 typer 依賴不該動既有行為）。

- [ ] **Step 6: Commit**

```bash
git add pyproject.toml
git commit -m "chore(deps): 加入 typer 依賴（批次 B1 準備）"
```

---

## Task 2: 建立 subprocess 黑箱測試層（argparse 時代）

**這層測試是跨越 argparse/Typer 邊界的 regression 防線——在 argparse 時代先寫好並全綠，遷移後必須仍全綠。只斷言我方可控的輸出（exit code、ASCII 前綴、payload 片段），不斷言框架內建訊息。**

**Files:**
- Create: `tests/test_cli_blackbox.py`

- [ ] **Step 1: 寫 subprocess 黑箱測試**

建立 `tests/test_cli_blackbox.py`，內容：

```python
"""Subprocess 黑箱測試——跨越 argparse/Typer 邊界的 regression 防線。

這些測試從 shell 真跑 `threads` CLI（透過 python -m），斷言：
- exit code
- stdout/stderr 中我方可控的輸出（[DRY RUN] / [OK] / [ERROR] / [WARN] 前綴 + payload 片段）

**不**斷言框架（argparse/Click）自動生成的 error 訊息逐字。
"""

import os
import subprocess
import sys

import pytest


def run_threads(args: list[str], env_extra: dict | None = None) -> subprocess.CompletedProcess:
    """跑 `python -m threads_pipeline.threads_cli.cli ...`，捕捉 stdout/stderr。"""
    env = os.environ.copy()
    # 設假 token，讓 require_token() 不先擋；真實 API 不會被呼叫（flag 組合在那之前就擋）
    env.setdefault("THREADS_ACCESS_TOKEN", "test_fake_token_not_real")
    if env_extra:
        env.update(env_extra)
    return subprocess.run(
        [sys.executable, "-m", "threads_pipeline.threads_cli.cli", *args],
        capture_output=True,
        text=True,
        env=env,
        encoding="utf-8",
    )


# === Group 1: 基本 help / version ===

def test_bbox_version():
    """threads --version → exit 0, 含版本號。"""
    r = run_threads(["--version"])
    assert r.returncode == 0
    assert "0.1.0" in r.stdout


def test_bbox_help():
    """threads --help → exit 0, 含 post / posts 字樣。"""
    r = run_threads(["--help"])
    assert r.returncode == 0
    assert "post" in r.stdout.lower()


def test_bbox_no_subcommand_prints_help():
    """threads（無 subcommand）→ exit 0 或 2（argparse 印 help 是 0；Typer 可能是 2），
    關鍵是 stdout/stderr 任一含 usage/help 訊息。"""
    r = run_threads([])
    # argparse: 我們 main() 有 parser.print_help() return 0
    # Typer: 預設也印 help，但 exit code 可能 0 或 2（視實作）
    assert r.returncode in (0, 2)
    combined = r.stdout + r.stderr
    assert "post" in combined.lower()


def test_bbox_unknown_subcommand_exits_2():
    """threads wrong → exit 2（argparse / Click 都是 2）。"""
    r = run_threads(["wrong_subcommand"])
    assert r.returncode == 2


# === Group 2: publish flag 組合 ===

def test_bbox_publish_dry_run_output():
    """threads post publish "text"（無 --confirm）→ dry-run，exit 0，stdout 含 [DRY RUN]。"""
    r = run_threads(["post", "publish", "hello world"])
    assert r.returncode == 0
    assert "[DRY RUN]" in r.stdout
    assert "hello world" in r.stdout


def test_bbox_publish_yes_without_confirm_exits_2():
    """threads post publish "x" --yes（無 --confirm）→ exit 2（flag 組合錯誤）。"""
    r = run_threads(["post", "publish", "x", "--yes"])
    assert r.returncode == 2
    # 我方輸出：stderr 含 [ERROR] 前綴
    assert "[ERROR]" in r.stderr


def test_bbox_publish_missing_text_exits_2():
    """threads post publish（缺 text）→ exit 2（框架層，只驗 exit code）。"""
    r = run_threads(["post", "publish"])
    assert r.returncode == 2


# === Group 3: reply flag 組合 ===

def test_bbox_reply_dry_run_output():
    """threads reply POST_123 "text"（無 --confirm）→ dry-run，exit 0，stdout 含 [DRY RUN]。"""
    r = run_threads(["reply", "POST_123", "my reply"])
    assert r.returncode == 0
    assert "[DRY RUN]" in r.stdout
    assert "POST_123" in r.stdout


def test_bbox_reply_yes_without_confirm_exits_2():
    """threads reply POST_123 "x" --yes（無 --confirm）→ exit 2。"""
    r = run_threads(["reply", "POST_123", "x", "--yes"])
    assert r.returncode == 2
    assert "[ERROR]" in r.stderr


def test_bbox_reply_missing_text_exits_2():
    """threads reply POST_123（缺 text）→ exit 2。"""
    r = run_threads(["reply", "POST_123"])
    assert r.returncode == 2


# === Group 4: publish-chain flag 組合 ===

def test_bbox_publish_chain_dry_run_output(tmp_path):
    """threads post publish-chain FILE（無 --confirm）→ dry-run，exit 0，stdout 含 [DRY RUN]。"""
    chain_file = tmp_path / "chain.txt"
    chain_file.write_text("第一則\n第二則\n", encoding="utf-8")
    r = run_threads(["post", "publish-chain", str(chain_file)])
    assert r.returncode == 0
    assert "[DRY RUN]" in r.stdout
    assert "2 posts" in r.stdout


def test_bbox_publish_chain_nonexistent_file_exits_1():
    """threads post publish-chain nonexistent.txt → exit 1（OSError）。"""
    r = run_threads(["post", "publish-chain", "/nonexistent/path/xyz.txt"])
    assert r.returncode == 1
    assert "[ERROR]" in r.stderr


def test_bbox_publish_chain_retry_policy_exits_2(tmp_path):
    """--on-failure retry（Level 1 未實作）→ exit 2（NotImplementedError）。"""
    chain_file = tmp_path / "chain.txt"
    chain_file.write_text("a\n", encoding="utf-8")
    r = run_threads([
        "post", "publish-chain", str(chain_file),
        "--confirm", "--yes", "--on-failure", "retry"
    ])
    assert r.returncode == 2


# === Group 5: 全域 flag ===

def test_bbox_json_flag_accepted():
    """--json 旗標應可被接受（batch A inert、batch B 會實作）→ 不 exit 2。"""
    r = run_threads(["--json", "post", "publish", "hello"])
    # 不管是 dry-run（0）還是其他合法 exit code，不應該是 unknown flag 的 2
    assert r.returncode == 0
    assert "hello" in r.stdout
```

- [ ] **Step 2: 跑 subprocess 黑箱測試驗證全綠（argparse 時代 baseline）**

```bash
python -m pytest tests/test_cli_blackbox.py -v
```

Expected: 全部測試 PASS（目前 argparse 實作應該能通過所有 case；若某個 fail，停下來先修正斷言使之符合當前 argparse 行為，再 commit——這是 **baseline**，必須反映當前真實行為）。

- [ ] **Step 3: 跑完整 test suite 驗證沒打破既有**

```bash
python -m pytest
```

Expected: 原 87 tests + 新 ~14 tests，全部 PASS。

- [ ] **Step 4: Commit**

```bash
git add tests/test_cli_blackbox.py
git commit -m "test(cli): 加入 subprocess 黑箱測試層作為 Typer 遷移的 regression 防線

這層測試只斷言我方可控的輸出（exit code、ASCII 前綴、payload
片段），跨越 argparse/Typer 邊界不變。遷移後此檔案內容不動、
必須仍全綠。"
```

---

## Task 3: 建立 Typer 骨架與新 module 檔案

**Files:**
- Modify: `threads_cli/cli.py`
- Create: `threads_cli/post.py`
- Create: `threads_cli/posts.py`
- Create: `threads_cli/account.py`

- [ ] **Step 1: 建立 `threads_cli/post.py`（空 module 骨架）**

```python
"""threads post 子命令群——publish / reply / publish-chain / (B2: insights / replies / delete)。"""

import typer

post_app = typer.Typer(help="Single post operations", no_args_is_help=True)
# Task 5/6/7 會在此檔加入 publish / reply / publish-chain handler
```

- [ ] **Step 2: 建立 `threads_cli/posts.py`（空骨架，B2 填）**

```python
"""threads posts 子命令群——search / list（B2 實作）。"""

import typer

posts_app = typer.Typer(help="Posts list operations (batch B2)", no_args_is_help=True)
# B2 實作 search / list
```

- [ ] **Step 3: 建立 `threads_cli/account.py`（空骨架，B2 填）**

```python
"""threads account 子命令群——info / insights（B2 實作）。"""

import typer

account_app = typer.Typer(help="Account operations (batch B2)", no_args_is_help=True)
# B2 實作 info / insights
```

- [ ] **Step 4: 改寫 `threads_cli/cli.py`——只保留 Typer app 宣告 + main()**

把整個 `threads_cli/cli.py` 換成：

```python
"""Threads CLI 入口——Typer app + subcommand groups。"""

import sys

# Windows UTF-8：reconfigure 在 Python 啟動後才有效
if sys.platform == "win32":
    sys.stdout.reconfigure(encoding="utf-8", errors="replace")
    sys.stderr.reconfigure(encoding="utf-8", errors="replace")

import typer

from threads_pipeline.threads_cli import __version__
from threads_pipeline.threads_cli.post import post_app
from threads_pipeline.threads_cli.posts import posts_app
from threads_pipeline.threads_cli.account import account_app

app = typer.Typer(
    name="threads",
    help="Threads API operations CLI (CLI-Anything compatible)",
    no_args_is_help=True,
    add_completion=False,
)

app.add_typer(post_app, name="post")
app.add_typer(posts_app, name="posts")
app.add_typer(account_app, name="account")


def _version_callback(value: bool):
    if value:
        typer.echo(f"threads {__version__}")
        raise typer.Exit()


@app.callback()
def _root(
    version: bool = typer.Option(
        False, "--version", callback=_version_callback, is_eager=True,
        help="Show version and exit"
    ),
    json_mode: bool = typer.Option(
        False, "--json", help="Output as JSON (B1: parsed; B2: 唯讀指令啟用完整 envelope)"
    ),
):
    """Root callback for global flags."""
    pass


def main(argv: list[str] | None = None) -> int:
    """CLI entry point（保持批次 A 簽章：吃 argv、回 exit code）。"""
    # 載入 .env（批次 A lessons 1.3）
    try:
        from threads_pipeline.main import _load_dotenv
        _load_dotenv()
    except Exception:
        pass

    try:
        app(args=argv, standalone_mode=False)
        return 0
    except typer.Exit as e:
        return e.exit_code
    except SystemExit as e:
        return int(e.code) if e.code is not None else 0


if __name__ == "__main__":
    sys.exit(main())
```

**備註**：
- `standalone_mode=False` 讓我們能捕捉 `typer.Exit` 並回傳 exit code，而不是讓 Typer 直接 `sys.exit()`——這是為了與批次 A 的 `main(argv) -> int` 簽章相容（既有測試的 in-process 呼叫仍能拿到 return value）。
- `reply` 指令在批次 A 是 top-level（`threads reply POST_123 "text"`）而非 `threads post reply`，Task 6 會把它加到 `post_app` 之外的 top-level 位置。

- [ ] **Step 5: 驗證 subprocess 黑箱測試不會全爆（此時遷移未完成，publish/reply/chain 還沒在 Typer 註冊）**

```bash
python -m pytest tests/test_cli_blackbox.py::test_bbox_version tests/test_cli_blackbox.py::test_bbox_help -v
```

Expected: `test_bbox_version` / `test_bbox_help` PASS（version 和 help 兩個應該 Typer 已經支援）。
其他 case 會 fail（handler 還沒遷移），這是預期的。

- [ ] **Step 6: Commit**

```bash
git add threads_cli/cli.py threads_cli/post.py threads_cli/posts.py threads_cli/account.py
git commit -m "refactor(cli): 建立 Typer app 骨架 + subcommand group modules

- cli.py 改為 Typer app 宣告 + 掛載 groups，保持 main(argv)->int 簽章
- post.py / posts.py / account.py 建立空骨架
- reply 指令移植延至 Task 6"
```

---

## Task 4: 遷移 publish 指令到 Typer

**Files:**
- Modify: `threads_cli/post.py`

- [ ] **Step 1: 在 `threads_cli/post.py` 加入 publish handler**

把 `threads_cli/post.py` 整個換成：

```python
"""threads post 子命令群——publish / reply / publish-chain。"""

import sys

import typer

from threads_pipeline.publisher import (
    publish_text,
    reply_to,
    publish_chain,
    PublishError,
    ChainMidwayError,
)
from threads_pipeline.threads_cli.output import error
from threads_pipeline.threads_cli.safety import (
    require_token,
    validate_confirm_yes,
    interactive_confirm,
)

post_app = typer.Typer(help="Single post operations", no_args_is_help=True)


@post_app.command("publish")
def publish_cmd(
    text: str = typer.Argument(..., help="Post text content"),
    confirm: bool = typer.Option(False, "--confirm", help="Actually publish (default: dry-run)"),
    yes: bool = typer.Option(False, "--yes", help="Skip interactive confirmation (Agent mode)"),
):
    """Publish a post."""
    token = require_token()
    is_tty = sys.stdin.isatty()
    validate_confirm_yes(confirm=confirm, yes=yes, is_tty=is_tty)

    char_count = len(text)

    # Dry-run 預設
    if not confirm:
        print("[DRY RUN] Would publish:")
        print("---------------------------------")
        print(text)
        print("---------------------------------")
        print(f"Character count: {char_count}")
        print("Add --confirm to actually publish.")
        return

    # 互動確認
    if not yes:
        print("About to publish:")
        print("---------------------------------")
        print(text)
        print("---------------------------------")
        print(f"Character count: {char_count}")
        if not interactive_confirm():
            print("(cancelled)")
            return

    # 真正發文
    try:
        post_id = publish_text(text, token=token)
    except PublishError as e:
        error(str(e), exit_code=1)

    print(f"[OK] Published as post {post_id}")


# Task 5/6: reply_cmd / publish_chain_cmd 加在下面
```

**備註**：
- Typer command function 回傳值會被忽略（exit code 透過 `raise typer.Exit()` 或讓函式正常結束來決定），所以不需要 `return 0`
- `error(...)` 仍透過 `sys.exit()` 結束，Typer 會正確捕捉到 exit code

- [ ] **Step 2: 跑 publish 相關 subprocess 黑箱測試**

```bash
python -m pytest tests/test_cli_blackbox.py -k publish -v
```

Expected:
- `test_bbox_publish_dry_run_output` PASS
- `test_bbox_publish_yes_without_confirm_exits_2` PASS
- `test_bbox_publish_missing_text_exits_2` PASS
- publish_chain 相關暫時 fail（Task 6 才遷移）— 這預期。

- [ ] **Step 3: Commit**

```bash
git add threads_cli/post.py
git commit -m "feat(cli): 遷移 post publish 指令到 Typer

subprocess 黑箱測試 publish 相關 case 全綠。"
```

---

## Task 5: 遷移 publish-chain 指令到 Typer

**Files:**
- Modify: `threads_cli/post.py`

- [ ] **Step 1: 在 `threads_cli/post.py` 加入 publish_chain handler**

在 `threads_cli/post.py` 檔尾（`# Task 5/6:` 註解處）追加：

```python
@post_app.command("publish-chain")
def publish_chain_cmd(
    file: str = typer.Argument(..., help="Input file (- for stdin)"),
    confirm: bool = typer.Option(False, "--confirm"),
    yes: bool = typer.Option(False, "--yes"),
    on_failure: str = typer.Option(
        "stop", "--on-failure",
        help="Mid-chain failure policy (Level 1 only implements 'stop')",
    ),
):
    """Publish a thread chain."""
    token = require_token()
    is_tty = sys.stdin.isatty()
    validate_confirm_yes(confirm=confirm, yes=yes, is_tty=is_tty)

    # 讀取輸入
    if file == "-":
        content = sys.stdin.read()
    else:
        try:
            with open(file, encoding="utf-8") as f:
                content = f.read()
        except OSError as e:
            error(f"Cannot read file {file}: {e}", exit_code=1)

    # 空行分隔規則
    texts = [line.strip() for line in content.splitlines() if line.strip()]
    if not texts:
        error("No non-empty lines in input", exit_code=1)

    total_chars = sum(len(t) for t in texts)

    # Dry-run
    if not confirm:
        print(f"[DRY RUN] Would publish chain of {len(texts)} posts:")
        print("---------------------------------")
        for i, t in enumerate(texts):
            label = "opener" if i == 0 else "reply"
            print(f"{i + 1}/{len(texts)} ({label}):\t{t} ({len(t)} chars)")
        print("---------------------------------")
        print(f"Total: {len(texts)} posts, {total_chars} chars")
        print(f"On-failure policy: {on_failure}")
        print("Add --confirm to actually publish.")
        return

    # 互動確認
    if not yes:
        print(f"About to publish chain of {len(texts)} posts:")
        for i, t in enumerate(texts):
            preview = t[:60] + ("..." if len(t) > 60 else "")
            print(f"  {i + 1}. {preview}")
        if not interactive_confirm():
            print("(cancelled)")
            return

    # 驗參：on_failure 必須 choices 之一（Typer 不像 argparse 有 choices 語法，手動驗）
    if on_failure not in ("stop", "retry", "rollback"):
        error(f"Invalid --on-failure value: {on_failure}", exit_code=2)

    # 真正發
    # IMPORTANT: ChainMidwayError IS-A PublishError — subclass 在前
    try:
        post_ids = publish_chain(texts, token=token, on_failure=on_failure)
    except NotImplementedError as e:
        error(str(e), exit_code=2)
    except ChainMidwayError as e:
        print(f"[ERROR] Chain failed at post {e.failed_index + 1}", file=sys.stderr)
        print(f"  Already posted IDs: {e.posted_ids}", file=sys.stderr)
        print(f"  Cause: {e.cause}", file=sys.stderr)
        print(f"  Recovery: manually delete or continue from post {e.failed_index + 1}", file=sys.stderr)
        raise typer.Exit(code=1)
    except PublishError as e:
        error(str(e), exit_code=1)

    print(f"[OK] Published chain of {len(post_ids)} posts:")
    for i, pid in enumerate(post_ids):
        print(f"  {i + 1}. {pid}")
```

- [ ] **Step 2: 跑 publish-chain 相關 subprocess 黑箱測試**

```bash
python -m pytest tests/test_cli_blackbox.py -k chain -v
```

Expected:
- `test_bbox_publish_chain_dry_run_output` PASS
- `test_bbox_publish_chain_nonexistent_file_exits_1` PASS
- `test_bbox_publish_chain_retry_policy_exits_2` PASS

- [ ] **Step 3: Commit**

```bash
git add threads_cli/post.py
git commit -m "feat(cli): 遷移 post publish-chain 指令到 Typer"
```

---

## Task 6: 遷移 reply 指令到 Typer（top-level）

**注意**：批次 A 的 `reply` 是 top-level（`threads reply POST_123 "text"`），不是 `threads post reply`。為保留行為，reply 掛在 root `app` 而非 `post_app`。

**Files:**
- Modify: `threads_cli/cli.py`
- Create: `threads_cli/reply.py`

- [ ] **Step 1: 建立 `threads_cli/reply.py`**

```python
"""threads reply 指令（top-level）。"""

import sys

import typer

from threads_pipeline.publisher import reply_to, PublishError
from threads_pipeline.threads_cli.output import error
from threads_pipeline.threads_cli.safety import (
    require_token,
    validate_confirm_yes,
    interactive_confirm,
)


def reply_cmd(
    post_id: str = typer.Argument(..., help="Parent post ID"),
    text: str = typer.Argument(..., help="Reply text"),
    confirm: bool = typer.Option(False, "--confirm"),
    yes: bool = typer.Option(False, "--yes"),
):
    """Reply to an existing post."""
    token = require_token()
    is_tty = sys.stdin.isatty()
    validate_confirm_yes(confirm=confirm, yes=yes, is_tty=is_tty)

    char_count = len(text)

    if not confirm:
        print(f"[DRY RUN] Would reply to post {post_id}:")
        print("---------------------------------")
        print(text)
        print("---------------------------------")
        print(f"Character count: {char_count}")
        print("Add --confirm to actually reply.")
        return

    if not yes:
        print(f"About to reply to post {post_id}:")
        print("---------------------------------")
        print(text)
        print("---------------------------------")
        if not interactive_confirm():
            print("(cancelled)")
            return

    try:
        reply_id = reply_to(post_id, text, token=token)
    except PublishError as e:
        error(str(e), exit_code=1)

    print(f"[OK] Reply posted as {reply_id} (parent: {post_id})")
```

- [ ] **Step 2: 在 `threads_cli/cli.py` 掛載 reply 為 top-level command**

**2a.** 在 `threads_cli/cli.py` 的上方 import 區（其他 `from threads_pipeline.threads_cli....` 行附近）加入：

```python
from threads_pipeline.threads_cli.reply import reply_cmd
```

**2b.** 在 `app.add_typer(account_app, name="account")` 這一行之後、`def _version_callback` 之前加入：

```python
app.command("reply")(reply_cmd)
```

完成後該區塊大概長這樣：

```python
app.add_typer(post_app, name="post")
app.add_typer(posts_app, name="posts")
app.add_typer(account_app, name="account")
app.command("reply")(reply_cmd)


def _version_callback(value: bool):
    ...
```

- [ ] **Step 3: 跑 reply 相關 subprocess 黑箱測試**

```bash
python -m pytest tests/test_cli_blackbox.py -k reply -v
```

Expected: 三個 reply case 全 PASS。

- [ ] **Step 4: 跑完整 subprocess 黑箱測試**

```bash
python -m pytest tests/test_cli_blackbox.py -v
```

Expected: 全部 ~14 cases PASS（代表 Typer 遷移在黑箱層面與 argparse 等效）。

- [ ] **Step 5: Commit**

```bash
git add threads_cli/cli.py threads_cli/reply.py
git commit -m "feat(cli): 遷移 reply 指令到 Typer（top-level）

subprocess 黑箱測試全綠（14 cases），Typer 遷移在外部行為
層面與 argparse 等效。"
```

---

## Task 7: 重寫 `tests/test_threads_cli.py` 配合 Typer handler signature

**背景**：批次 A 的 `test_threads_cli.py` 用 `patch("threads_pipeline.threads_cli.cli.publish_text")` 直接 patch cli.py 裡 import 的名稱，再呼叫 `main([...])`。Typer 遷移後 `publish_text` 不再 import 到 `cli.py` 而是 `post.py`，且 handler 不再是 `cmd_post_publish(args)` 形式而是 Typer command function。

重寫用 Typer 官方推薦的 `CliRunner` 模式 + patch 正確路徑。

**Files:**
- Modify: `tests/test_threads_cli.py`

- [ ] **Step 1: 整個重寫 `tests/test_threads_cli.py`**

把 `tests/test_threads_cli.py` 整個換成：

```python
"""CLI 層單元測試——Typer CliRunner 模式。"""

from unittest.mock import patch

import pytest
from typer.testing import CliRunner

from threads_pipeline.threads_cli.cli import app

runner = CliRunner()


# === publish ===

def test_cli_post_publish_dry_run():
    """無 --confirm 應 dry-run，不呼叫 publisher。"""
    with patch("threads_pipeline.threads_cli.post.publish_text") as mock_pub, \
         patch("threads_pipeline.threads_cli.post.require_token", return_value="fake"):
        result = runner.invoke(app, ["post", "publish", "測試文字"])

    assert result.exit_code == 0
    assert mock_pub.call_count == 0
    assert "[DRY RUN]" in result.output


def test_cli_post_publish_confirm_yes_calls_api():
    """--confirm --yes 應呼叫 publish_text。"""
    with patch("threads_pipeline.threads_cli.post.publish_text", return_value="POST_ABC") as mock_pub, \
         patch("threads_pipeline.threads_cli.post.require_token", return_value="fake"):
        result = runner.invoke(app, ["post", "publish", "hello", "--confirm", "--yes"])

    assert result.exit_code == 0
    mock_pub.assert_called_once()
    assert mock_pub.call_args.args[0] == "hello"


def test_cli_post_publish_yes_without_confirm_exit_2():
    """--yes 無 --confirm → exit 2。"""
    with patch("threads_pipeline.threads_cli.post.require_token", return_value="fake"):
        result = runner.invoke(app, ["post", "publish", "x", "--yes"])
    assert result.exit_code == 2


# === reply ===

def test_cli_reply_dry_run():
    """reply 無 --confirm 應 dry-run。"""
    with patch("threads_pipeline.threads_cli.reply.reply_to") as mock_rep, \
         patch("threads_pipeline.threads_cli.reply.require_token", return_value="fake"):
        result = runner.invoke(app, ["reply", "POST_123", "我的回覆"])

    assert result.exit_code == 0
    assert mock_rep.call_count == 0
    assert "[DRY RUN]" in result.output


def test_cli_reply_confirm_yes_calls_api():
    """reply --confirm --yes 應呼叫 reply_to。"""
    with patch("threads_pipeline.threads_cli.reply.reply_to", return_value="REPLY_ABC") as mock_rep, \
         patch("threads_pipeline.threads_cli.reply.require_token", return_value="fake"):
        result = runner.invoke(app, ["reply", "POST_123", "回覆", "--confirm", "--yes"])

    assert result.exit_code == 0
    mock_rep.assert_called_once()
    assert mock_rep.call_args.args[0] == "POST_123"
    assert mock_rep.call_args.args[1] == "回覆"


# === publish-chain ===

def test_cli_publish_chain_reads_file(tmp_path):
    """從檔案讀多行，dry-run 印出清單。"""
    chain_file = tmp_path / "chain.txt"
    chain_file.write_text("第一則\n第二則\n第三則\n", encoding="utf-8")

    with patch("threads_pipeline.threads_cli.post.publish_chain") as mock_chain, \
         patch("threads_pipeline.threads_cli.post.require_token", return_value="fake"):
        result = runner.invoke(app, ["post", "publish-chain", str(chain_file)])

    assert result.exit_code == 0
    assert mock_chain.call_count == 0
    assert "3 posts" in result.output


def test_cli_publish_chain_confirm_yes_calls_api(tmp_path):
    """--confirm --yes 應呼叫 publish_chain。"""
    chain_file = tmp_path / "chain.txt"
    chain_file.write_text("a\nb\nc\n", encoding="utf-8")

    with patch("threads_pipeline.threads_cli.post.publish_chain", return_value=["1", "2", "3"]) as mock_chain, \
         patch("threads_pipeline.threads_cli.post.require_token", return_value="fake"):
        result = runner.invoke(app, ["post", "publish-chain", str(chain_file), "--confirm", "--yes"])

    assert result.exit_code == 0
    mock_chain.assert_called_once()
    assert mock_chain.call_args.args[0] == ["a", "b", "c"]


def test_cli_publish_chain_on_failure_not_stop_exit_2(tmp_path):
    """--on-failure=retry 應因 NotImplementedError 而 exit 2。"""
    chain_file = tmp_path / "chain.txt"
    chain_file.write_text("a\n", encoding="utf-8")

    with patch("threads_pipeline.threads_cli.post.require_token", return_value="fake"):
        result = runner.invoke(app, [
            "post", "publish-chain", str(chain_file),
            "--confirm", "--yes", "--on-failure", "retry"
        ])
    assert result.exit_code == 2


# === subprocess-based tests 保留（Typer 也應支援）===

def test_threads_cli_version():
    """threads --version 應印出版本號。"""
    import subprocess
    import sys
    result = subprocess.run(
        [sys.executable, "-m", "threads_pipeline.threads_cli.cli", "--version"],
        capture_output=True, text=True,
    )
    assert result.returncode == 0
    assert "0.1.0" in result.stdout


def test_threads_cli_help():
    """threads --help 應印使用說明且 exit 0。"""
    import subprocess
    import sys
    result = subprocess.run(
        [sys.executable, "-m", "threads_pipeline.threads_cli.cli", "--help"],
        capture_output=True, text=True,
    )
    assert result.returncode == 0
    assert "post" in result.stdout.lower()
```

- [ ] **Step 2: 跑重寫後的 in-process tests**

```bash
python -m pytest tests/test_threads_cli.py -v
```

Expected: 10 tests 全 PASS。

- [ ] **Step 3: 跑完整 test suite 確認總綠**

```bash
python -m pytest
```

Expected: 全綠（subprocess 黑箱 ~14 + in-process cli ~10 + cli_safety + publisher + analyzer + threads_client + report + advisor + db_helpers = 原 87 + 新黑箱 ~14 = ~101 tests，全 PASS）。

- [ ] **Step 4: Commit**

```bash
git add tests/test_threads_cli.py
git commit -m "test(cli): 重寫 CLI 單元測試配合 Typer handler signature

改用 Typer CliRunner 模式 + patch 新的 import 路徑
(threads_cli.post / threads_cli.reply)。subprocess-based
tests 保留不變。"
```

---

## Task 8: 手動 smoke 驗證 + merge 準備

**Files:** 無（純驗證）

- [ ] **Step 1: 驗證 `threads --version` 和 `threads --help`**

```bash
threads --version
threads --help
threads post --help
threads posts --help
threads account --help
```

Expected:
- `threads --version` → `threads 0.1.0`
- `threads --help` → 列出 post / posts / account / reply subcommands
- `threads post --help` → 列出 publish / publish-chain
- `threads posts --help` / `threads account --help` → 空 group 或 B2 pending 提示（此時沒有子指令）

- [ ] **Step 2: Dry-run 測試（不打 API）**

```bash
threads post publish "這是測試訊息"
threads reply FAKE_POST_ID "測試回覆"
echo -e "第一則\n第二則" | threads post publish-chain -
```

Expected: 每個都印 `[DRY RUN]` 並 exit 0。

- [ ] **Step 3: 跑批次 A smoke 流程（照 `docs/superpowers/handoffs/2026-04-15-threads-cli-smoke.md`）**

照該文件每一步重跑一次。關鍵 checkpoint：
- Token scope GET /me（零副作用）
- dry-run 不發
- Error-path（Token missing、flag 組合錯誤）

不需要真實發文（那是 B1 要不要發的決定，建議**不真發**——B1 是純遷移、沒改行為，smoke dry-run 已足夠；真發留給 B2 新指令一起做）。

- [ ] **Step 4: 寫 B1 smoke handoff**

建立 `docs/superpowers/handoffs/2026-04-XX-batch-b1-smoke.md`（`XX` 用實際日期），格式仿 `2026-04-15-threads-cli-smoke.md`，內容記錄：
- B1 範圍（Typer 遷移 + 既有指令回歸）
- subprocess 黑箱測試 N cases 全綠
- 重寫後 in-process tests 10 cases 全綠
- 手動 dry-run 測試結果
- 批次 A smoke 重跑結果
- 尚未真發（遷移段不含新 API 行為，故不真發）

- [ ] **Step 5: Commit smoke handoff**

```bash
git add docs/superpowers/handoffs/2026-04-XX-batch-b1-smoke.md
git commit -m "docs(handoff): 批次 B1 smoke 驗證通過紀錄"
```

- [ ] **Step 6: Push 分支並開 PR（或合併到 main，視使用者習慣）**

```bash
git push -u origin feat/cli-batch-b1
```

提供使用者 PR URL 或告知「已 push，要不要 merge 到 main」。

---

## Self-Review

### Spec coverage

對照 `docs/superpowers/specs/2026-04-16-cli-batch-b-design.md` 的 B1 部分：

- ✅ `typer` 依賴加到 pyproject.toml — Task 1
- ✅ Subprocess 黑箱測試 10-15 case — Task 2（14 cases）
- ✅ 既有 87 tests 重寫配合 Typer — Task 7
- ✅ Typer integration test（CliRunner）— Task 7
- ✅ 批次 A smoke 重跑 — Task 8
- ✅ B1 commit series（feat/cli-batch-b1 分支）— Task 1 開分支、Task 8 push

### 行為保留契約（Spec Section 3）

- ✅ Positional 參數順序不變（publish text、reply post_id text、publish-chain file）
- ✅ Flag 名稱不變（--confirm / --yes / --on-failure）
- ✅ Exit code 0/1/2 不變（subprocess 黑箱測試驗證）
- ✅ `[ERROR]` / `[OK]` / `[DRY RUN]` 前綴保留（handler 中 print 邏輯全照舊）
- ✅ `publish-chain` 吃 `drafts/*.txt` 不變
- ✅ `publish-chain` rollback 不做（Task 5 保持 NotImplementedError）

### 已知 Typer 行為差異（容忍範圍內）

- `threads wrong_subcommand` → Typer 是 `Error: No such command 'wrong_subcommand'.` + exit 2；argparse 是 `invalid choice: 'wrong_subcommand'` + exit 2。**exit code 相同、訊息不同**——subprocess 黑箱測試只驗 exit code。
- `threads` 無 subcommand → Typer 預設 `no_args_is_help=True` 印 help 並 exit 0；argparse 的 main() 手動 `parser.print_help()` 後 return 0。行為等效。
- `threads post publish` 缺 text → Typer 是 `Missing argument 'TEXT'.` + exit 2；argparse 是 `the following arguments are required: text` + exit 2。**exit code 相同**。

### Type consistency

- `main(argv: list[str] | None = None) -> int` 簽章 Task 3 定義、Task 7 測試驗證
- `publish_cmd` / `publish_chain_cmd` / `reply_cmd` 是 Typer command（return None），透過 `app(args=argv, standalone_mode=False)` 統一回 exit code
- `error(..., exit_code=N)` 仍是 `sys.exit()`，Typer main() 中的 try/except SystemExit 能接到

### 風險 checkpoint

| 風險 | 緩解 |
|---|---|
| Typer 與 argparse 對 `--flag=value` vs `--flag value` 處理不同 | subprocess 黑箱測試有涵蓋常見 flag 組合 |
| `standalone_mode=False` + `typer.Exit` 捕捉可能遺漏某些 exit path | Task 7 CliRunner tests + Task 2 subprocess tests 雙重驗證 |
| `app.add_typer(...)` 與 `app.command("reply")(reply_cmd)` 混用可能有 help 排版怪異 | 接受（批次 A lessons 允許「框架自動 help 文字可變」） |

### 不在 B1 範圍（留給 B2）

- `posts search / list`
- `post insights / replies`
- `account info / insights`
- `post delete` + `.deleted_posts/` 備份機制
- `--json` envelope 完整實作（warnings 欄位）
- Posts search CJK 偵測

### 最後 checklist

- [ ] Task 1-8 全部標 `[x]` 完成
- [ ] `python -m pytest` 全綠
- [ ] `python -m pytest tests/test_cli_blackbox.py` 全綠
- [ ] `threads --version` / `threads --help` 能跑
- [ ] `feat/cli-batch-b1` 分支已 push
- [ ] B1 smoke handoff 已寫
- [ ] 使用者授權 merge 後才 merge 到 main
